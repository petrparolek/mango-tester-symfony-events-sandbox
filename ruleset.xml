<?xml version="1.0"?>
<ruleset>
    <!-- Extending rulesets -->
    <rule ref="./vendor/contributte/qa/ruleset.xml"/>

    <arg name="extensions" value="php,phpt"/>

    <!-- Specific rules -->
    <rule ref="SlevomatCodingStandard.Files.TypeNameMatchesFileName">
        <properties>
            <property name="rootNamespaces" type="array">
                <element key="app" value="App"/>
                <element key="tests/cases/integration/presenters" value="AppTests\Presenters"/>
                <element key="tests/src" value="AppTests"/>
            </property>
            <property name="extensions" type="array">
                <element value="php"/>
                <element value="phpt"/>
            </property>
        </properties>
    </rule>

    <rule ref="./vendor/contributte/qa/ruleset.xml">
        <!-- Exclude specific rules from imported ruleset -->
        <exclude name="SlevomatCodingStandard.Classes.ClassStructure.IncorrectGroupOrder" />
    </rule>

    <rule ref="SlevomatCodingStandard.Namespaces.ReferenceUsedNamesOnly.ReferenceViaFullyQualifiedName">
        <exclude-pattern>tests/*.php</exclude-pattern>
    </rule>

</ruleset>
